// w.a.p to find the sum of two numbers using functon

//function definition
sum(2,1)
function sum(a,b)
{
    // add=a+b;
    // console.log(`sum of two numbers:${add}`);

    const add= a+b; // if any variable inside the function to get in outside  we can use return statemet  
    return add;
    // console.log(); DO NOT work anything after return statement. 
}
//function call
result=sum(10,20)
console.log(`sum of two numbers:${result}`);
// console.log(add(20,30));

// if we use const variable its using return variable to disply


